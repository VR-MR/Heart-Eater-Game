#pragma strict

private var rb: Rigidbody;

function getPlayerPosition() {
	return Camera.main.gameObject.transform.position;
}

function randomPosition(center, xVar, yVar) {
//	return Vector3(center.x + xVar * Math.random(), center.y + yVar * Math.random(), center.z);
}

function Start () {
//	pos = getPlayerPosition();
//	print(pos);
//	var targetPos = randomPosition(pos, 0.5, 0.5);
//	print(targetPos);
//
//	rb = GetComponent.<Rigidbody>();
//	var movement = Vector3(0, 200, -200);
//	rb.AddForce (movement);
}


function Update () {

}